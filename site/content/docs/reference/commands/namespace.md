---
# Generated by tools/gen-site-docs from internal/cli. Do not edit.
title: "kx namespace"
linkTitle: "namespace"
description: "List namespaces, or switch to an indexed one; alias: kx ns."
weight: 16
---

Lists namespaces, or switches to one by index.

The listing is saved to a slot of its own, outside the `kx get` history, so an index here keeps meaning the same entry however much you have listed since. `kx state --targets` shows the slot without listing again.

To act on a namespace rather than switch to it — describe, label, delete — list it with `kx get ns`, which stacks it like any other listing.

## Usage

```text
kx namespace [OPTIONS] [index]
```

## Aliases

`kx ns`

## Arguments

| Argument | Description |
|---|---|
| `[index]` | Row number from the current listing; run kx state to see it |

## Global options

| Option | Description |
|---|---|
| `--no-color` | Disable styled output |
| `-h, --help` | Show this message and exit |

## Examples

```bash
kx namespace
kx namespace 2
```
