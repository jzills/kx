---
# Generated by tools/gen-site-docs from internal/cli. Do not edit.
title: "kx exec"
linkTitle: "exec"
description: "Open an interactive shell in an indexed pod (bash, falling back to sh)."
weight: 11
---

Runs a command inside an indexed pod. With no command, tries each configured shell in turn — bash, then sh, unless the shells key in the config file says otherwise.

Unrecognized flags are passed through to kubectl.

## Usage

```text
kx exec [OPTIONS] <index> [kubectl flags] [-- command...]
```

## Arguments

| Argument | Description |
|---|---|
| `<index>` | Row number from the current listing; run kx state to see it |
| `[command]...` | Command to run in the pod instead of a shell |

## Global options

| Option | Description |
|---|---|
| `--no-color` | Disable styled output |
| `-h, --help` | Show this message and exit |

## Examples

```bash
kx exec 1
kx exec 1 -- ls /app
kx exec 1 -c sidecar
```
