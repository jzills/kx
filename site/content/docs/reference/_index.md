---
title: Reference
description: Every command, argument and flag, generated from the CLI itself.
weight: 4
---

The pages under [Commands](commands/) are generated from the kx command tree by
`tools/gen-site-docs`, so the flags documented here are the flags the binary
accepts — a check in CI fails if they fall out of step.

`kx <command> --help` prints the same reference in the terminal.

{{< kx-children >}}
